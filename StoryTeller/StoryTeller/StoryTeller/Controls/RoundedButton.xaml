<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xe="clr-namespace:XamEffects;assembly=XamEffects"
             x:Name="this"
             x:Class="StoryTeller.Controls.RoundedButton">
    <xe:BorderView CornerRadius="6" BackgroundColor="{StaticResource ButtonClicked}"
               xe:TouchEffect.Color="{StaticResource DarkButtonClicked}"
xe:Commands.Tap="{Binding Command}"
          BindingContext="{x:Reference this}"
        HeightRequest="42">
        <Label Text="{Binding Text}"
             VerticalOptions="Center" 
             HorizontalOptions="Center" 
             FontSize="16" FontFamily="JetBrainsMono" 
             TextColor="{StaticResource Foreground}"/>
    </xe:BorderView>
</ContentView>